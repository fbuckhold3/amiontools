% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cached_fetch.R
\name{use_amion_data_cached}
\alias{use_amion_data_cached}
\title{Try loading amiontools' aggregate summaries from the REDCap app_cache
record instead of fetching Amion + RDM live.}
\usage{
use_amion_data_cached(
  rdm_token,
  redcap_url,
  cache_record_id = Sys.getenv("CACHE_RECORD_ID"),
  max_age_hours = 192
)
}
\arguments{
\item{rdm_token}{RDM REDCap API token (test or prod — caller's choice).}

\item{redcap_url}{REDCap API URL.}

\item{cache_record_id}{Record ID of the app_cache record
(default: \code{CACHE_RECORD_ID} env var).}

\item{max_age_hours}{Passed through to \code{gmed::load_cached_amion()}.
Default 192h (8 days) — the cache refreshes weekly.}
}
\value{
A list with \code{rotation}, \code{team}, \code{talloc} reactives
(each shaped like the matching \code{build_*_summary()} return value)
on a cache hit, or \code{NULL} on a cache miss/stale/malformed payload
— callers should fall back to \code{use_amion_data()} in that case.
}
\description{
Try loading amiontools' aggregate summaries from the REDCap app_cache
record instead of fetching Amion + RDM live.
}
