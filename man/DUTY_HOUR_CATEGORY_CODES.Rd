% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/duty_hour_entries.R
\name{DUTY_HOUR_CATEGORY_CODES}
\alias{DUTY_HOUR_CATEGORY_CODES}
\title{RDM \code{duty_hour_log} field choice codes -> labels, exactly matching the
dictionary written 2026-09-12 (\code{redcap-dictionary-edit}) — REDCap's API
requires the raw numeric code on WRITE for dropdown fields (reads can use
raw_or_label="label", writes cannot), so any write path needs this map.
Centralized here (not duplicated in imslu.ind.dash) since amiontools
already owns the category vocabulary everywhere else in this package.}
\usage{
DUTY_HOUR_CATEGORY_CODES
}
\description{
RDM \code{duty_hour_log} field choice codes -> labels, exactly matching the
dictionary written 2026-09-12 (\code{redcap-dictionary-edit}) — REDCap's API
requires the raw numeric code on WRITE for dropdown fields (reads can use
raw_or_label="label", writes cannot), so any write path needs this map.
Centralized here (not duplicated in imslu.ind.dash) since amiontools
already owns the category vocabulary everywhere else in this package.
}
