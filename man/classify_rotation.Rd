% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classify.R
\name{classify_rotation}
\alias{classify_rotation}
\title{Roll up a vector of raw Amion \code{Grouping} values into top-level rotation
categories via ROTATION_CATEGORY_MAP. Anything not in the map comes back
as "UNMAPPED" rather than silently dropping/guessing — callers should
treat any UNMAPPED rows as a sign the map needs a new entry (Amion's
Grouping vocabulary can grow, e.g. a new rotation or renamed team).}
\usage{
classify_rotation(grouping)
}
\arguments{
\item{grouping}{Character vector of raw Amion \code{Grouping} values.}
}
\description{
Roll up a vector of raw Amion \code{Grouping} values into top-level rotation
categories via ROTATION_CATEGORY_MAP. Anything not in the map comes back
as "UNMAPPED" rather than silently dropping/guessing — callers should
treat any UNMAPPED rows as a sign the map needs a new entry (Amion's
Grouping vocabulary can grow, e.g. a new rotation or renamed team).
}
