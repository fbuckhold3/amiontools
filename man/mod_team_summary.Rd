% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mod_team_summary.R
\name{mod_team_summary_ui}
\alias{mod_team_summary_ui}
\alias{mod_team_summary}
\alias{mod_team_summary_server}
\title{Team-assignment summary module — UI + server.}
\usage{
mod_team_summary_ui(id)

mod_team_summary_server(
  id,
  resident_id,
  rdm_token,
  redcap_url,
  amion_lo = AMION_LO_DEFAULT,
  crosswalk_r = NULL,
  amion_r = NULL,
  summary_r = NULL
)
}
\arguments{
\item{id}{Module namespace id.}

\item{resident_id}{Reactive returning the currently-selected RDM
record_id (character/numeric coercible).}

\item{rdm_token}{RDM REDCap API token (test or prod — caller's choice).}

\item{redcap_url}{REDCap API URL.}

\item{amion_lo}{Amion Lo= program token; defaults to AMION_LO_DEFAULT.}

\item{crosswalk_r, amion_r}{Optional reactives (e.g. from
use_amion_data()) returning pre-fetched crosswalk/Amion data — pass
these when composing this module alongside others in one session to
fetch once instead of each module independently re-fetching. NULL
(default): fetches its own data, same as before.}

\item{summary_r}{Optional reactive (e.g. from
\code{use_amion_data_cached()}) returning a pre-built
\code{list(team_summary_wide=, class_avg_wide=, off_summary_wide=,
off_class_avg_wide=)} — same shape \code{build_team_summary()} returns.
When supplied, skips both the live fetch (crosswalk_r/amion_r ignored)
AND the local aggregation entirely. NULL (default): unchanged
live-fetch behavior.}
}
\description{
Team-assignment summary module — UI + server.
}
