% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/expected_attendance.R
\name{classify_expected_conference}
\alias{classify_expected_conference}
\title{Classify expected conference site for a vector of (rotation category,
clinic-session) pairs.}
\usage{
classify_expected_conference(category, clinic_sessions = NA_character_)
}
\arguments{
\item{category}{Character vector — rolled-up rotation category, e.g. from
\code{classify_rotation(daily_detail$Rotation)}.}

\item{clinic_sessions}{Character vector, same length — the raw
\code{Clinic_Sessions} value from \code{build_daily_detail()} (";"-
joined if more than one that day), used only to detect the CC-VA-pm
excusal. NA/empty is fine (means "no c-type session that day").}
}
\value{
Character vector: "SLUH", "VA", or "None". Anything not in
\code{EXPECTED_CONFERENCE_MAP} (including "UNMAPPED" from
\code{classify_rotation()}) comes back "None" rather than guessed.
}
\description{
Classify expected conference site for a vector of (rotation category,
clinic-session) pairs.
}
