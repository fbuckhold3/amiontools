% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mod_attendance_reconciliation.R
\name{mod_attendance_reconciliation_ui}
\alias{mod_attendance_reconciliation_ui}
\alias{mod_attendance_reconciliation}
\alias{mod_attendance_reconciliation_server}
\title{Attendance-reconciliation module — UI + server.}
\usage{
mod_attendance_reconciliation_ui(id)

mod_attendance_reconciliation_server(
  id,
  resident_id,
  rdm_token,
  redcap_url,
  amion_lo = AMION_LO_DEFAULT,
  crosswalk_r = NULL,
  amion_r = NULL,
  expected_calendar_r = NULL
)
}
\arguments{
\item{id}{Module namespace id.}

\item{resident_id}{Reactive returning the currently-selected RDM
record_id (character/numeric coercible).}

\item{rdm_token}{RDM REDCap API token (test or prod — caller's choice).}

\item{redcap_url}{REDCap API URL.}

\item{amion_lo}{Amion Lo= program token; defaults to AMION_LO_DEFAULT.}

\item{crosswalk_r, amion_r}{Optional reactives (e.g. from
use_amion_data()) returning pre-fetched crosswalk/Amion data — pass
these to fetch once instead of independently re-fetching. Ignored
when \code{expected_calendar_r} is also supplied. NULL (default): fetches
its own data.}

\item{expected_calendar_r}{Optional reactive (e.g. from
\code{use_expected_calendar_cached()}) returning the cached, expanded
expected-conference calendar — when supplied, skips the live Amion
fetch entirely for this section (questions_log stays live regardless).
NULL (default): computed live from crosswalk_r/amion_r.}
}
\description{
Attendance-reconciliation module — UI + server.
}
