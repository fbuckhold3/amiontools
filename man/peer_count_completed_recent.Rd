% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/peer_review_counts.R
\name{peer_count_completed_recent}
\alias{peer_count_completed_recent}
\title{Count peer_eval instances a resident has completed (as evaluator) within
a trailing window.}
\usage{
peer_count_completed_recent(
  evaluator_id,
  redcap_url,
  rdm_token,
  days,
  as_of = Sys.Date()
)
}
\arguments{
\item{evaluator_id}{The resident's own record_id (peer_evaluator_id to
match against).}

\item{redcap_url}{REDCap API base URL.}

\item{rdm_token}{REDCap API token (test or prod — caller's choice).}

\item{days}{Trailing window size in days (inclusive of today).}

\item{as_of}{Reference "today" — defaults to \code{Sys.Date()}.}
}
\value{
A list: \code{n} (count within the window) and \code{n_total} (all-time
count, same figure \code{.peer_count_completed()} would return).
}
\description{
Count peer_eval instances a resident has completed (as evaluator) within
a trailing window.
}
