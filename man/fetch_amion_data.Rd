% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch.R
\name{fetch_amion_data}
\alias{fetch_amion_data}
\title{Fetch all Amion year-blocks and return a single combined data frame.
Use fetch_amion_month() instead when only one month is needed — much
cheaper than a full academic-year pull.}
\usage{
fetch_amion_data(
  amion_lo = Sys.getenv("AMION_LO", AMION_LO_DEFAULT),
  urls = NULL
)
}
\arguments{
\item{amion_lo}{The Lo= value (program token) from Amion}

\item{urls}{Optional vector of full URLs; otherwise auto-built from the
start AY through the current AY (covers past + current year).}
}
\description{
Fetch all Amion year-blocks and return a single combined data frame.
Use fetch_amion_month() instead when only one month is needed — much
cheaper than a full academic-year pull.
}
